#include <bits/stdc++.h>
using namespace std;
int main(){
    string s = "catsanddogs";
    vector<string> dict = {"cats", "cat", "and", "sand", "dogs", "dog"};
    
    int len = s.length();
    for(int i = 0; i < len -1; i++){
        
    }
}